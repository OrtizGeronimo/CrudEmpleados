#crear db (primero start server)
#(java -jar derbyrun.jar ij)
connect 'jdbc:derby://localhost:1527/ProyectoFinalDB;create=true';
#conectar db
connect 'jdbc:derby://localhost:1527/ProyectoFinalDB';

#crear tablas
CREATE TABLE TIPO_EMPLEADO( ID INTEGER NOT NULL, DESCRIPCION VARCHAR (100), PRIMARY KEY (ID));


CREATE TABLE EMPLEADOS( LEGAJO INTEGER, NOMBRE VARCHAR(100) NOT NULL, APELLIDO VARCHAR(100) NOT NULL, EMAIL VARCHAR(255) NOT NULL, SSN VARCHAR (20) NOT NULL, SALARIO_SEMANAL FLOAT, TARIFA_HORA FLOAT, HORAS FLOAT, VENTAS_BRUTAS FLOAT, TARIFA_COMISION FLOAT, SALARIO_BASE FLOAT, TIPO INTEGER, PRIMARY KEY (LEGAJO), FOREIGN KEY (TIPO) REFERENCES TIPO_EMPLEADO(ID));

#insertar 

INSERT INTO TIPO_EMPLEADO (ID, DESCRIPCION) VALUES (1, ASALARIADO);
INSERT INTO TIPO_EMPLEADO (ID, DESCRIPCION) VALUES (2, POR_HORAS);
INSERT INTO TIPO_EMPLEADO (ID, DESCRIPCION) VALUES (1, POR_COMISION);
INSERT INTO TIPO_EMPLEADO (ID, DESCRIPCION) VALUES (1, BASE_MAS_COMISION);